[build]
  command = "pnpm install --frozen-lockfile && pnpm --filter @workspace/bac-master-elite build"
  publish = "artifacts/bac-master-elite/dist"

[build.environment]
  NODE_VERSION = "22"
  NPM_FLAGS = "--version"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
